import { Wishlist_me_wishlist_edges_node } from "@nautical/queries/gqlTypes/Wishlist";
// import * as H from "history";

export interface IProps {
  wishlist?: Wishlist_me_wishlist_edges_node[] | null;
  // history: H.History;
}
