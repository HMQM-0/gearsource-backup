export interface DesignerData {
  name: string;
  jsonContent: any | null;
}

export interface MenuStyle {
  active: boolean;
  barColor: string;
  borderColor: string;
  hoverColor: string;
  textColor: string;
}
